diff --git a/node_modules/@office-ai/aioncli-core/.bun-tag-a322f8e24a95bb08 b/node_modules/@office-ai/aioncli-core/.bun-tag-a322f8e24a95bb08
new file mode 100644
index 0000000..e69de29
diff --git a/node_modules/@office-ai/aioncli-core/.bun-tag-d5fdf500a8c912 b/node_modules/@office-ai/aioncli-core/.bun-tag-d5fdf500a8c912
new file mode 100644
index 0000000..e69de29
diff --git a/node_modules/@office-ai/aioncli-core/.bun-tag-e430e8d9d3acdb6c b/node_modules/@office-ai/aioncli-core/.bun-tag-e430e8d9d3acdb6c
new file mode 100644
index 0000000..e69de29
diff --git a/node_modules/@office-ai/aioncli-core/dist/src/core/openaiContentGenerator.js b/node_modules/@office-ai/aioncli-core/dist/src/core/openaiContentGenerator.js
index 2bdb1c5..80918be 100644
--- a/node_modules/@office-ai/aioncli-core/dist/src/core/openaiContentGenerator.js
+++ b/node_modules/@office-ai/aioncli-core/dist/src/core/openaiContentGenerator.js
@@ -644,6 +644,19 @@ export class OpenAIContentGenerator {
                         result[key] = value;
                     }
                 }
+                else if (key === 'properties' && value && typeof value === 'object' && !Array.isArray(value)) {
+                    // `properties` is a name->subschema MAP, not a schema node. Recurse
+                    // each child schema, but never run the map itself through convertTypes:
+                    // its trailing "has properties -> add type:object" guard would inject a
+                    // bogus `"type":"object"` property when a tool has a field literally
+                    // named "properties" (e.g. Notion's notion-create-pages), producing an
+                    // invalid subschema and a `400 Invalid schema for function` from the API.
+                    const props = {};
+                    for (const [pName, pSchema] of Object.entries(value)) {
+                        props[pName] = convertTypes(pSchema);
+                    }
+                    result[key] = props;
+                }
                 else if (typeof value === 'object') {
                     result[key] = convertTypes(value);
                 }
@@ -1527,17 +1540,38 @@ export class OpenAIContentGenerator {
         const modelName = this.model.toLowerCase();
         const isKimiModel = modelName.includes('kimi-k2.5') ||
             (modelName.includes('kimi') && modelName.includes('k2.5'));
-        const isRestrictedModel = modelName.includes('gpt-5') ||
+        // OpenAI reasoning models — the gpt-5.x flagship family AND the o-series
+        // (o1/o3/o4, incl. -mini/-preview) — reject top_p, temperature!=1, the
+        // penalty knobs, logprobs and max_tokens; they accept only temperature=1
+        // and max_completion_tokens. The `\bo[134]\b` guard matches o1/o3/o4
+        // without catching gpt-4o (no word boundary before the trailing `o`).
+        const isReasoningModel = modelName.includes('gpt-5') ||
             modelName.includes('gpt5') ||
+            /\bo[134]\b/.test(modelName);
+        const isRestrictedModel = isReasoningModel ||
             modelName.includes('gpt-4o') ||
             modelName.includes('gpt4o') ||
             isKimiModel;
         if (isRestrictedModel) {
             params.temperature = 1.0;
-            // Kimi K2.5 models enforce top_p=0.95
             if (isKimiModel) {
+                // Kimi K2.5 models enforce top_p=0.95
                 params.top_p = 0.95;
             }
+            else if (isReasoningModel) {
+                // Strip the sampling params OpenAI reasoning models reject
+                // (`400 Unsupported parameter: 'top_p'`), and migrate any token cap
+                // to the parameter they do accept (max_completion_tokens).
+                delete params.top_p;
+                delete params.top_k;
+                delete params.frequency_penalty;
+                delete params.presence_penalty;
+                delete params.repetition_penalty;
+                if (params.max_tokens !== undefined && params.max_completion_tokens === undefined) {
+                    params.max_completion_tokens = params.max_tokens;
+                    delete params.max_tokens;
+                }
+            }
         }
         return params;
     }
diff --git a/node_modules/@office-ai/aioncli-core/dist/src/mcp/oauth-provider.js b/node_modules/@office-ai/aioncli-core/dist/src/mcp/oauth-provider.js
index 51ed849..df00348 100644
--- a/node_modules/@office-ai/aioncli-core/dist/src/mcp/oauth-provider.js
+++ b/node_modules/@office-ai/aioncli-core/dist/src/mcp/oauth-provider.js
@@ -147,6 +147,18 @@ export class MCPOAuthProvider {
      * @returns Object containing the port (available immediately) and a promise for the auth response
      */
     startCallbackServer(expectedState, port) {
+        // Wayland: the callback server is pinned to a single port (57000), so a
+        // retried or concurrent OAuth login would collide with EADDRINUSE and
+        // wedge the port for 5 minutes ("Authorization failed: address already
+        // in use"). Close any prior in-flight login's server before binding so a
+        // new attempt always supersedes a stale one.
+        if (this._activeCallbackServer) {
+            try {
+                this._activeCallbackServer.close();
+            }
+            catch (e) { /* already closed */ }
+            this._activeCallbackServer = null;
+        }
         let portResolve;
         let portReject;
         const portPromise = new Promise((resolve, reject) => {
@@ -168,16 +180,46 @@ export class MCPOAuthProvider {
                     const error = url.searchParams.get('error');
                     if (error) {
                         res.writeHead(HTTP_OK, { 'Content-Type': 'text/html' });
-                        res.end(`
-              <html>
-                <body>
-                  <h1>Authentication Failed</h1>
-                  <p>Error: ${error.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</p>
-                  <p>${(url.searchParams.get('error_description') || '').replace(/</g, '&lt;').replace(/>/g, '&gt;')}</p>
-                  <p>You can close this window.</p>
-                </body>
-              </html>
-            `);
+                        res.end(`<!doctype html>
+<html lang="en">
+<head>
+<meta charset="utf-8" />
+<meta name="viewport" content="width=device-width, initial-scale=1" />
+<title>Connection didn't complete</title>
+<style>
+*{box-sizing:border-box}html,body{height:100%;margin:0}
+body{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px;
+background:radial-gradient(900px 520px at 50% -4%,rgba(255,90,90,.10),transparent 60%),#0a0a0a;
+color:#f5f5f5;font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",system-ui,sans-serif;-webkit-font-smoothing:antialiased}
+.card{width:100%;max-width:460px;text-align:center;padding:48px 40px;border-radius:22px;
+background:rgba(255,255,255,.02);border:1px solid rgba(255,90,90,.20);
+box-shadow:0 30px 90px rgba(0,0,0,.55),0 0 0 1px rgba(255,255,255,.02) inset}
+.mark{width:78px;height:78px;margin:0 auto 26px;position:relative}
+.mark .ring{position:absolute;inset:0;border-radius:50%;
+background:radial-gradient(circle at 50% 42%,rgba(255,90,90,.26),rgba(255,90,90,.05) 70%);
+box-shadow:0 0 44px rgba(255,90,90,.30)}
+.mark svg{position:absolute;inset:0;margin:auto}
+.brand{font-size:12px;letter-spacing:.16em;text-transform:uppercase;color:#8a8a8a}
+.brand b{color:#fff;font-weight:700;letter-spacing:.02em;text-transform:none;font-size:15px}
+h1{font-size:22px;font-weight:700;margin:16px 0 0;color:#fff}
+p{margin:10px 0 0;font-size:14px;line-height:1.55;color:#9a9a9a;word-break:break-word}
+.detail{margin-top:14px;font-size:12.5px;color:#ff7b7b;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
+</style>
+</head>
+<body>
+<div class="card">
+<div class="mark"><div class="ring"></div>
+<svg width="78" height="78" viewBox="0 0 78 78" fill="none">
+<circle cx="39" cy="39" r="23" stroke="#ff5a5a" stroke-width="2.4" opacity=".9"/>
+<path d="M32 32l14 14M46 32l-14 14" stroke="#ff5a5a" stroke-width="3.2" stroke-linecap="round"/>
+</svg></div>
+<div class="brand">Connecting to <b>Wayland</b></div>
+<h1>Connection didn't complete</h1>
+<p>Head back to Wayland and try connecting again.</p>
+<div class="detail">${error.replace(/</g, '&lt;').replace(/>/g, '&gt;')}${(url.searchParams.get('error_description') ? ' &middot; ' + url.searchParams.get('error_description') : '').replace(/</g, '&lt;').replace(/>/g, '&gt;')}</div>
+</div>
+</body>
+</html>`);
                         server.close();
                         reject(new Error(`OAuth error: ${error}`));
                         return;
@@ -196,15 +238,49 @@ export class MCPOAuthProvider {
                     }
                     // Send success response to browser
                     res.writeHead(HTTP_OK, { 'Content-Type': 'text/html' });
-                    res.end(`
-            <html>
-              <body>
-                <h1>Authentication Successful!</h1>
-                <p>You can close this window and return to Gemini CLI.</p>
-                <script>window.close();</script>
-              </body>
-            </html>
-          `);
+                    res.end(`<!doctype html>
+<html lang="en">
+<head>
+<meta charset="utf-8" />
+<meta name="viewport" content="width=device-width, initial-scale=1" />
+<title>Connected to Wayland</title>
+<style>
+*{box-sizing:border-box}html,body{height:100%;margin:0}
+body{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px;
+background:radial-gradient(900px 520px at 50% -4%,rgba(255,107,53,.12),transparent 60%),#0a0a0a;
+color:#f5f5f5;font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",system-ui,sans-serif;-webkit-font-smoothing:antialiased}
+.card{width:100%;max-width:460px;text-align:center;padding:48px 40px;border-radius:22px;
+background:rgba(255,255,255,.02);border:1px solid rgba(255,107,53,.18);
+box-shadow:0 30px 90px rgba(0,0,0,.55),0 0 0 1px rgba(255,255,255,.02) inset}
+.mark{width:78px;height:78px;margin:0 auto 26px;position:relative}
+.mark .ring{position:absolute;inset:0;border-radius:50%;
+background:radial-gradient(circle at 50% 42%,rgba(255,107,53,.30),rgba(255,107,53,.05) 70%);
+box-shadow:0 0 44px rgba(255,107,53,.38)}
+.mark svg{position:absolute;inset:0;margin:auto}
+.brand{font-size:12px;letter-spacing:.16em;text-transform:uppercase;color:#8a8a8a}
+.brand b{color:#fff;font-weight:700;letter-spacing:.02em;text-transform:none;font-size:15px}
+h1{font-size:22px;font-weight:700;margin:16px 0 0;color:#fff}
+p{margin:10px 0 0;font-size:14.5px;line-height:1.55;color:#9a9a9a}
+.hint{margin-top:22px;font-size:12.5px;color:#5f5f5f}
+.tag{margin-top:28px;font-size:10.5px;letter-spacing:.24em;text-transform:uppercase;color:#3f3f3f}
+</style>
+</head>
+<body>
+<div class="card">
+<div class="mark"><div class="ring"></div>
+<svg width="78" height="78" viewBox="0 0 78 78" fill="none">
+<circle cx="39" cy="39" r="23" stroke="#ff6b35" stroke-width="2.4" opacity=".9"/>
+<path d="M29.5 39.6l6.6 6.6L49 32.4" stroke="#ff6b35" stroke-width="3.3" stroke-linecap="round" stroke-linejoin="round"/>
+</svg></div>
+<div class="brand">Connected to <b>Wayland</b></div>
+<h1>Connection successful</h1>
+<p>This integration is now linked to Wayland.<br/>You can close this tab and head back to the app.</p>
+<div class="hint">This window will close automatically.</div>
+<div class="tag">Perceives &middot; Reasons &middot; Acts &middot; Evolves</div>
+</div>
+<script>setTimeout(function(){try{window.close()}catch(e){}},2200)</script>
+</body>
+</html>`);
                     server.close();
                     resolve({ code, state });
                 }
@@ -213,6 +289,7 @@ export class MCPOAuthProvider {
                     reject(error);
                 }
             });
+            this._activeCallbackServer = server;
             server.on('error', (error) => {
                 portReject(error);
                 reject(error);
